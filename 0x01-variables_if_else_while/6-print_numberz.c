#include <stdio.h>

/**
 * main - The primary function of this program
 *
 * Return: Always 0
 */
int main(void)
{
	int alpha;

	for (alpha = 48; alpha <= 57; alpha++)
	{
		putchar(alpha);
	}
	putchar(10); /*New line*/

	return (0);
}
